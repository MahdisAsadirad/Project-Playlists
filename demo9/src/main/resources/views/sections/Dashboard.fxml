<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.demo9.Controller.DashboardController"
            style="-fx-background-color: #f8f9fa;">

    <left>
        <VBox fx:id="sidebar" spacing="15" style="-fx-background-color: white; -fx-padding: 20; -fx-pref-width: 250;">
            <Label text="🎵 Playlist Manager" style="-fx-font-size: 20; -fx-font-weight: bold; -fx-text-fill: #333; -fx-padding: 0 0 20 0;"/>

            <VBox spacing="10">
                <Button text="My Playlists" fx:id="playlistsBtn" style="-fx-sidebar-button" onAction="#showPlaylistsSection"/>
                <Button text="🎵 Songs Library" fx:id="songsBtn" style="-fx-sidebar-button" onAction="#showSongsSection"/>
                <Button text="Merge Playlists" fx:id="mergeBtn" style="-fx-sidebar-button" onAction="#showMergeSection"/>
                <Button text="Shuffle Merge" fx:id="shuffleBtn" style="-fx-sidebar-button" onAction="#showShuffleSection"/>
                <Button text="Filter Playlists" fx:id="filterBtn" style="-fx-sidebar-button" onAction="#showFilterSection"/>
                <Button text="Sort Playlists" fx:id="sortBtn" style="-fx-sidebar-button" onAction="#showSortSection"/>
                <Button text="❤Liked Songs" fx:id="likedBtn" style="-fx-sidebar-button" onAction="#showLikedSection"/>
            </VBox>

            <VBox spacing="10" style="-fx-padding: 20 0 0 0;">
                <Label fx:id="userLabel" style="-fx-font-weight: bold; -fx-text-fill: #666;"/>
                <Button text="🚪 Logout"
                        style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-pref-width: 100%; -fx-padding: 10;"
                        onAction="#handleLogout"/>
            </VBox>
        </VBox>
    </left>

    <center>
        <StackPane fx:id="contentArea">
        </StackPane>
    </center>

</BorderPane>