<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox prefHeight="600" prefWidth="800" spacing="20" style="-fx-padding: 30;" styleClass="root" stylesheets="@page-styles.css" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="org.example.demo9.Controller.ShuffleController">

    <!-- عنوان صفحه -->
    <Label styleClass="title" text="🎲 Shuffle Merge Playlists" />

    <!-- بخش انتخاب پلی‌لیست‌ها و نام پلی‌لیست جدید -->
    <VBox prefHeight="366.0" prefWidth="739.0" spacing="15" styleClass="card">
        <Label styleClass="subtitle" text="Select multiple playlists to shuffle merge:" />

        <VBox prefHeight="214.0" prefWidth="748.0" spacing="10">
            <Label styleClass="subtitle" text="Available Playlists:" />
            <ListView fx:id="playlistsListView" prefHeight="85.0" prefWidth="699.0" styleClass="text-field" />
            <Label styleClass="hint" text="Hold Ctrl to select multiple playlists" />
        </VBox>

        <VBox spacing="5">
            <Label styleClass="subtitle" text="New Playlist Name:" />
            <TextField fx:id="newPlaylistNameField" prefWidth="300" promptText="Enter name for shuffled playlist" styleClass="text-field" />
        </VBox>

        <HBox spacing="10">
            <Button fx:id="shuffleButton" styleClass="button success" text="Create Shuffled Playlist" />
            <Button fx:id="reshuffleButton" styleClass="button" text="Reshuffle" />
        </HBox>
    </VBox>

    <!-- نتایج -->
    <VBox fx:id="resultContainer" spacing="10" />

    <!-- لیست آهنگ‌های شافل شده -->
    <VBox prefHeight="400" spacing="10">
        <Label styleClass="subtitle" text="🎵 Shuffled Songs:" />
        <ScrollPane fitToWidth="true" prefHeight="350" styleClass="scroll-pane">
            <VBox fx:id="shuffledSongsContainer" spacing="10" style="-fx-padding: 15;" styleClass="card" />
        </ScrollPane>
    </VBox>

</VBox>
