<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.demo9.Controller.ShuffleController"
      spacing="20" style="-fx-padding: 30;">

    <Label text="🎲 Shuffle Merge Playlists" style="-fx-font-size: 24; -fx-font-weight: bold; -fx-text-fill: #333;"/>

    <VBox spacing="15" style="-fx-padding: 20; -fx-background-color: white; -fx-background-radius: 10;">
        <Label text="Select multiple playlists to shuffle merge:" style="-fx-font-size: 16; -fx-font-weight: bold;"/>

        <VBox spacing="10">
            <Label text="Available Playlists:" style="-fx-font-weight: bold;"/>
            <ListView fx:id="playlistsListView" prefHeight="200" prefWidth="400"/>
            <Label text="Hold Ctrl to select multiple playlists" style="-fx-text-fill: #666; -fx-font-size: 12;"/>
        </VBox>

        <VBox spacing="5">
            <Label text="New Playlist Name:" style="-fx-font-weight: bold;"/>
            <TextField fx:id="newPlaylistNameField" promptText="Enter name for shuffled playlist" prefWidth="300"/>
        </VBox>

        <Button fx:id="shuffleButton" text="Create Shuffled Playlist" prefWidth="200"/>
    </VBox>

    <VBox fx:id="resultContainer" spacing="10">
        <!-- نتایج اینجا نمایش داده می‌شود -->
    </VBox>

</VBox>