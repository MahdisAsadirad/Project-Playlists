<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.demo9.Controller.FilterController"
      spacing="20" style="-fx-padding: 30;">

    <Label text="📊 Filter Playlist" style="-fx-font-size: 24; -fx-font-weight: bold; -fx-text-fill: #333;"/>

    <VBox spacing="15" style="-fx-padding: 20; -fx-background-color: white; -fx-background-radius: 10;">
        <Label text="Filter songs from a playlist:" style="-fx-font-size: 16; -fx-font-weight: bold;"/>

        <HBox spacing="15" alignment="CENTER_LEFT">
            <VBox spacing="5">
                <Label text="Select Playlist:" style="-fx-font-weight: bold;"/>
                <ComboBox fx:id="playlistCombo" prefWidth="200"/>
            </VBox>

            <VBox spacing="5">
                <Label text="Filter Criteria:" style="-fx-font-weight: bold;"/>
                <ComboBox fx:id="criteriaCombo" prefWidth="150"/>
            </VBox>
        </HBox>

        <VBox spacing="5">
            <Label text="Filter Value:" style="-fx-font-weight: bold;"/>
            <TextField fx:id="filterValueField" promptText="Enter value to filter by" prefWidth="300"/>
            <Label text="Example: For 'Genre' enter 'Rock', for 'Release Year' enter '2020'"
                   style="-fx-text-fill: #666; -fx-font-size: 12;"/>
        </VBox>

        <VBox spacing="5">
            <Label text="New Playlist Name:" style="-fx-font-weight: bold;"/>
            <TextField fx:id="newPlaylistNameField" promptText="Enter name for filtered playlist" prefWidth="300"/>
        </VBox>

        <Button fx:id="filterButton" text="Create Filtered Playlist" prefWidth="180"/>
    </VBox>

    <VBox fx:id="resultContainer" spacing="10">
        <!-- نتایج اینجا نمایش داده می‌شود -->
    </VBox>

</VBox>