<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/11.0.1"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.demo9.Controller.FilterController"
      spacing="20"
      stylesheets="@page-styles.css"
      styleClass="root"
      style="-fx-padding: 30;">

    <!-- عنوان -->
    <Label text="📊 Filter Playlist" styleClass="title"/>

    <!-- فرم فیلتر -->
    <VBox spacing="15" styleClass="card">
        <Label text="Filter songs from a playlist:" styleClass="subtitle"/>

        <HBox spacing="15" alignment="CENTER_LEFT">
            <!-- انتخاب پلی‌لیست -->
            <VBox spacing="5">
                <Label text="Select Playlist:" styleClass="subtitle"/>
                <ComboBox fx:id="playlistCombo" prefWidth="200" styleClass="combo-box"/>
            </VBox>

            <!-- معیار فیلتر -->
            <VBox spacing="5">
                <Label text="Filter Criteria:" styleClass="subtitle"/>
                <ComboBox fx:id="criteriaCombo" prefWidth="150" styleClass="combo-box"/>
            </VBox>
        </HBox>

        <!-- مقدار فیلتر -->
        <VBox spacing="5">
            <Label text="Filter Value:" styleClass="subtitle"/>
            <TextField fx:id="filterValueField" promptText="Enter value to filter by" prefWidth="300" styleClass="text-field"/>
            <Label text="Example: For 'Genre' enter 'Rock', for 'Release Year' enter '2020'" styleClass="hint"/>
        </VBox>

        <!-- نام پلی‌لیست جدید -->
        <VBox spacing="5">
            <Label text="New Playlist Name:" styleClass="subtitle"/>
            <TextField fx:id="newPlaylistNameField" promptText="Enter name for filtered playlist" prefWidth="300" styleClass="text-field"/>
        </VBox>

        <!-- دکمه ایجاد -->
        <Button fx:id="filterButton" text="Create Filtered Playlist" styleClass="button success"/>
    </VBox>

    <!-- نمایش نتیجه -->
    <VBox fx:id="resultContainer" spacing="10" styleClass="card"/>
</VBox>
